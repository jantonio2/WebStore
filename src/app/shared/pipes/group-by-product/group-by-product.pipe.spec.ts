import { GroupByProductPipe } from './group-by-product.pipe';

describe('GroupByProductPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByProductPipe();
    expect(pipe).toBeTruthy();
  });
});
